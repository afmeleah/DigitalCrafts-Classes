# def func(a, b):
#     value = a + b
#     return value

# print(func(4,5))

def total_count(any):
    list_length = len(total_count())
    total_chars = len(total_count())
    return {"list_length" : list_length, "total_chars" : total_chars  }

total_count(["I", "am", "number", "one"])